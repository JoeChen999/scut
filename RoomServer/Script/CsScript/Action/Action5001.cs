//----------------------------------------------------------------------------------------------------
// This code was auto generated by tools.
// You may need to modify 'TakeAction' method.
// Don't modify the rest unless you know what you're doing.
//----------------------------------------------------------------------------------------------------

using System;
using ZyGames.Framework.Common.Serialization;
using ZyGames.Framework.Game.Contract;
using ZyGames.Framework.Game.Contract.Action;
using ZyGames.Framework.Game.Service;

namespace Genesis.GameServer.RoomServer
{
    public class Action5001 : BaseStruct
    {
        private CRGetRoomInfo m_RequestPacket;
        private RCGetRoomInfo m_ResponsePacket;

        private RoomManager m_RoomManager;
        private int m_UserId;

        public Action5001(ActionGetter actionGetter)
            : base((short)5001, actionGetter)
        {
            m_RequestPacket = null;
            m_ResponsePacket = null;
            m_UserId = 0;
        }

        public override bool GetUrlElement()
        {
            m_UserId = actionGetter.GetSession().UserId;
            m_RequestPacket = ProtoBufUtils.Deserialize<CRGetRoomInfo>((byte[])actionGetter.GetMessage());
            return true;
        }

        public override bool TakeAction()
        {
            m_RoomManager = RoomManager.GetInstance(m_RequestPacket.RoomId);
            return true;
        }

        public override void WriteResponse(BaseGameResponse response)
        {
            m_RoomManager.PushIntoMessageQueue(actionGetter.GetActionId(), actionGetter.GetSession(), m_RequestPacket);
        }
    }
}
